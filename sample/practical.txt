% ==============================================================================
% SINGAPORE-CAMBRIDGE GCE A-LEVEL H2 COMPUTING (9569/02)
% OFFICIAL SPECIMEN / EXEMPLAR PAPER 2 (LAB-BASED PRACTICAL)
% ==============================================================================

\noindent Your program code and output for each of Task 1 to 4 should be saved in a single \texttt{.ipynb} file. For example, your program code and output for Task 1 should be saved as:\par\vspace{0.4em}
\noindent\texttt{TASK1\_<your name>\_<centre number>\_<index number>.ipynb}\par\vspace{1.0em}

\maintask{1}

A denary number can be converted to binary using the division by 2 method. The denary number is divided by 2 repeatedly until it becomes 0. After each division, the remainder of 1 or 0 is stored. These remainders are written in reverse order to create the binary number. For example, converting the denary number 28 to binary:

\begin{center}
\begin{tabular}{r@{\ }l}
28 / 2 = 14 & remainder 0 \\
14 / 2 = 7 & remainder 0 \\
7 / 2 = 3 & remainder 1 \\
3 / 2 = 1 & remainder 1 \\
1 / 2 = 0 & remainder 1 \\
\end{tabular}
\end{center}
28 denary is 11100 in binary.

A denary number can be converted to binary using the division by 2 method and a stack.

The stack can store any number of integer data items using a 1-dimensional list. The top of stack pointer stores the index of the next space to store data in the stack.

The data is not deleted when it is popped from the stack; the top of stack pointer is changed to point to the next space to store data.

\tasksubtaskintro{1}

\subtask{1.1}

The function \code{push()} takes the stack, the top of stack pointer and the data item to push onto the stack as parameters. The function stores the data item in the stack and updates the top of stack pointer. The function returns the top of stack pointer. If the stack is full, the length of the list should be increased.

Write program code for the function \code{push()}. \Marks{4}

\subtask{1.2}

The function \code{pop()} takes the stack and the top of stack pointer as parameters. The data item at the top of the stack is returned along with the updated top of stack pointer.

The data is not deleted when it is popped. The top of stack pointer is changed to point to the new top of stack; this allows data to be overwritten when new data is pushed onto the stack.

If the stack is empty, the function returns \code{False}.

Write program code for the function \code{pop()}. \Marks{4}

\subtask{1.3}

The function \code{main()} declares a 1-dimensional list to represent the stack and initialises the top of stack pointer to 0.

The function takes a positive integer as input from the user or as a parameter.

The function only outputs to the screen and does not return any data.

If the number input is 0, the program outputs the binary value 0.

If the number input is greater than 0, the input is repeatedly divided by 2 until it reaches 0; each remainder is pushed onto the stack using the function \code{push()} you created in Task 1.1. Each digit is then popped from the stack to create the binary number using the function \code{pop()} you created in Task 1.2. The binary number is then output on one line.

Write program code for \code{main()} and call this function. \Marks{10}

Test your program with the following inputs:
\begin{taskitemize}
  \item Test 1: 0
  \item Test 2: 22
  \item Test 3: 46967
\end{taskitemize}

Show the output for each test. \Marks{2}

\taskfooter{1}

\newpage
\TurnOver

\maintask{2}

A prototype for a computer game needs to be created using Object-Oriented Programming.

The game has a player who has to move through a series of rooms. Each room has at least one door to one other room. In all rooms apart from the Start room, a user has to answer a question correctly before he can move to the room he has chosen.

Each room has four positions where a door can be situated: North, South, East and West. Not all rooms have a door in each position. In the game, each room is given a number; the Start room is room number 0.

\tasksubtaskintro{2}

\subtask{2.1}

The class \code{Room} contains four attributes:
\begin{taskitemize}
  \item \code{north\_exit} -- an integer for the room number that the North door leads to, or -1 if there is no door.
  \item \code{east\_exit} -- an integer for the room number that the East door leads to, or -1 if there is no door.
  \item \code{south\_exit} -- an integer for the room number that the South door leads to, or -1 if there is no door.
  \item \code{west\_exit} -- an integer for the room number that the West door leads to, or -1 if there is no door.
\end{taskitemize}

The class \code{Room} has the following methods:
\begin{taskitemize}
  \item A constructor that takes four integers for \code{north\_exit}, \code{east\_exit}, \code{south\_exit} and \code{west\_exit} as parameters. Each parameter is assigned to the appropriate attribute.
  \item \code{get\_exit()} that takes a character as a parameter (\code{'N'}, \code{'S'}, \code{'E'}, or \code{'W'}) to represent the door selected and returns the attribute value for that door.
\end{taskitemize}

Write program code to declare the class \code{Room} and its methods. \Marks{5}

Three classes inherit from \code{Room}: \code{Start}, \code{Finish} and \code{Normal}.

\code{Start} is the first room where the game begins and also contains:
\begin{taskitemize}
  \item the attribute \code{start\_text} that is output at the beginning of the game. This value is initialised in the constructor from a parameter.
  \item the method \code{get\_message()} that returns the string in \code{start\_text} and a statement for each direction where there is a door from that room, for example \code{'There is a door to the North.'} The method also returns the string \code{"Start"} (for example as part of a tuple) to indicate that this is the Start room.
\end{taskitemize}

Write program code to declare the class \code{Start} and its methods. \Marks{5}

\code{Finish} is the room where the game ends and also contains:
\begin{taskitemize}
  \item the attribute \code{finish\_text} that is output at the end of the game. This value is initialised in the constructor from a parameter.
  \item the method \code{get\_message()} that returns \code{finish\_text} and the string \code{"Finish"} (for example as part of a tuple) to indicate that the game is finished.
\end{taskitemize}

Write program code to declare the class \code{Finish} and its methods. \Marks{2}

\code{Normal} is a room that is not the Start room and is not the Finish room. The class \code{Normal} also contains:
\begin{taskitemize}
  \item the attributes \code{question} and \code{answer}. The value of each is initialised in the constructor from its parameter.
  \item the method \code{get\_question()} that returns the data in \code{question}.
  \item the method \code{check\_answer()} that compares its parameter (the user's answer) to \code{answer} and returns \code{True} if it matches and \code{False} if it does not match.
  \item the method \code{get\_message()} that returns a string describing all of the doors from that room. The method also returns the string \code{"Normal"} (for example as part of a tuple).
\end{taskitemize}

Write program code to declare the class \code{Normal} and its methods. \Marks{4}

\subtask{2.2}

The text file \code{rooms.txt} stores data about the rooms in the game. Each row of data is a separate room.

The values in each row are in the order:
\begin{taskitemize}
  \item room type: \code{Start}, \code{Finish}, or \code{Normal}
  \item the room number that can be accessed to the North of the room, or -1 if there is no door
  \item the room number that can be accessed to the East of the room, or -1 if there is no door
  \item the room number that can be accessed to the South of the room, or -1 if there is no door
  \item the room number that can be accessed to the West of the room, or -1 if there is no door
  \item the start message if the room is of type \code{Start}, or finish message if of type \code{Finish}, or question if of type \code{Normal}
  \item the answer to the question if the room is of type \code{Normal}
\end{taskitemize}

The rooms are read in the order they appear in the file and stored in a 1-dimensional list.

Write program code for a function to:
\begin{taskitemize}
  \item read the data from the text file
  \item create an object of the appropriate class for each room
  \item store the rooms in a 1-dimensional list
  \item return the 1-dimensional list.
\end{taskitemize} \Marks{7}

\subtask{2.3}

When the game is played, the message in the Start room is output which includes the position of the door(s).

The user inputs the door to go through until he provides a valid door. The message for the new room is output which includes the position of the door(s) in that room. When the user selects a door, the question for the room he is in is output. The user needs to enter his answer until he provides the correct answer.

The process repeats until the user reaches the Finish room. When the user reaches the Finish room, the message for the room is output and the game ends.

Write program code for a function to allow a user to play the game and call this function. \Marks{13}

Test your program with given sample inputs. \Marks{2}

\taskfooter{2}

\newpage
\TurnOver

\maintask{3}

A social media website allows users to create accounts. Users can create posts; these are messages that are displayed to other users. Users can comment on posts.

A database stores data about users, the posts and comments on each post from the social media website.

Three tables in the database are shown:

\noindent\code{User (\uline{Username}, Blocked)}\par
\noindent\code{Post (\uline{PostID}, PostText, DateTimePosted, \dashuline{Username})}\par
\noindent\code{Comment (\uline{CommentID}, \dashuline{PostID}, \dashuline{Username}, TheText, DateTimeCommented)}\par\vspace{0.4em}

A primary key is indicated by underlining one or more attributes. Foreign keys are indicated by using a dashed underline.

The database is being created and tested using sample data which is given in the following text files: \code{user.txt}, \code{post.txt} and \code{comment.txt}.

The field \code{Blocked} in the table \code{User} stores 1 if the user is currently blocked and stores 0 if the user is not blocked.

\tasksubtaskintro{3}

\subtask{3.1}

Write a Python program that uses SQL to create a database with the three tables: \code{User}, \code{Post} and \code{Comment}.

Define the primary and foreign keys for each table. \Marks{6}

\subtask{3.2}

Write a Python program that uses SQL to insert the data from each of the three text files into the appropriate fields in each database table. \Marks{4}

Run your program. \Marks{1}

\subtask{3.3}

Write a Python function that:
\begin{taskitemize}
  \item uses SQL to search for all comments by a user who is blocked
  \item outputs each returned username and comment on a new line.
\end{taskitemize} \Marks{5}

Run your program. \Marks{1}

\taskfooter{3}

\newpage
\TurnOver

\maintask{4}

A computer program prototype is being developed to store records in a hash table.

Each record has a key field (positive integer) and a string data item.

The hash table is set up as a list, initialised with capacity for 100 records.

The hashing algorithm is: $\text{hash}(k) = k \pmod s$

$k$ represents the key field and $s$ represents the maximum size of the hash table.

\tasksubtaskintro{4}

\subtask{4.1}

Write program code to initialise the global list \code{hash\_table} with 100 empty records. \Marks{1}

\subtask{4.2}

Write program code for the function \code{calculate\_hash()} to take a key field as a parameter and return the calculated hash value using the given hashing algorithm. \Marks{2}

\subtask{4.3}

The hash table uses linear probing when there is a collision. Linear probing is when the next index is checked to identify whether the index is free. This repeats until either a free index is found, or all indices have been checked and the list is full.

Write program code for the function \code{insert()} to:
\begin{taskitemize}
  \item take a key field and string data item as parameters
  \item calculate the index using the \code{calculate\_hash()} function
  \item store the key field and string data item in the index if available, otherwise use linear probing to find the next free index
  \item return \code{True} if the record was successfully stored or \code{False} if the hash table is full.
\end{taskitemize} \Marks{6}

\subtask{4.4}

The text file \code{records.txt} stores 70 key fields and the associated string data item. For this prototype, the string data item is a single word. The key field and string data item are separated by a space.

Write program code for the function \code{read\_data()} to:
\begin{taskitemize}
  \item read each key field and string data item from the text file
  \item call the function \code{insert()} with each key field and string data item
  \item output an appropriate message if any record cannot be stored in the hash table.
\end{taskitemize} \Marks{4}

Call the function \code{read\_data()} and then output the contents of the hash table. \Marks{1}

\subtask{4.5}

Write program code for the function \code{find\_record()}:
\begin{taskitemize}
  \item take a key field as a parameter
  \item use its hash value to return the string data item for that record or return \code{False} if the record is not stored in the hash table.
\end{taskitemize} \Marks{3}

\subtask{4.6}

Write program code to call the function \code{read\_data()} again and call the function \code{find\_record()} with the key field 475.

If the record is found, output the string data item in an appropriate message. If the record is not found, output \code{"Record not found"}. \Marks{1}

Test your program. \Marks{1}

\taskfooter{4}